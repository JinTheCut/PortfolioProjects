
# Greek Search Frontend (Starter)

A static HTML/CSS/JS page that demo-searches a small JSON file of tokens. This shows a concordance-style UI without a backend.

## Run
Open `index.html` in a browser. No server required.

## What to extend next
- Replace JSON with a real API
- Add diacritic-insensitive search
- Add pagination and highlighting
